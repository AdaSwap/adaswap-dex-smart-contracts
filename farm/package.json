{
    "name": "@adaswap/farm",
    "version": "0.0.1",
    "description": "📖 Farm contracts for AdaSwap dApp",
    "files": [
        "contracts",
        "artifacts"
    ],
    "scripts": {
        "test": "yarn hardhat test --network hardhat",
        "compile": "yarn hardhat compile",
        "chain": "hardhat node --network hardhat",
        "local": "hardhat run --network localhost scripts/deploy.js",
        "deploy-testnet": "yarn hardhat run --network milkomedaTestnet scripts/deploy.js",
        "deploy-mainnet": "yarn hardhat run --network milkomedaMainnet scripts/deploy.js",
        "deploy-dev": "yarn hardhat run --network hardhat scripts/deploy.js"
    },
    "license": "MIT",
    "devDependencies": {
        "@nomiclabs/hardhat-ethers": "^2.0.6",
        "@nomiclabs/hardhat-waffle": "^2.0.3",
        "@openzeppelin/contracts": "^4.7.3",
        "chai": "^4.3.6",
        "dotenv": "^16.0.1",
        "ethereum-waffle": "^3.4.4",
        "ethers": "^5.6.9",
        "hardhat": "^2.9.9",
        "hardhat-contract-sizer": "^2.6.1"
    }
}
