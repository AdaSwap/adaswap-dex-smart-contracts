{
  "name": "@adaswap-testnet/periphery",
  "version": "0.0.1",
  "description": "📖 Solidity libraries that are shared across Uniswap contracts",
  "files": [
    "contracts",
    "build",
    "!contracts/test"
  ],
  "scripts": {
    "test": "yarn hardhat test --network development",
    "ganache": "npx ganache --chain.networkId=200101 --chain.chainId=1 --chain.vmErrorsOnRPCResponse --chain.hardfork=istanbul --miner.defaultGasPrice=20000000000 --miner.blockGasLimit=6721975 --server.port=7545 --wallet.mnemonic='height school space unique renew cable one stumble ring cube artefact plunge' --wallet.defaultBalance=10000",
    "test-headless": "npm run ganache > /dev/null & npm run test && kill $(lsof -t -i:7545) || kill $(lsof -t -i:8545)",
    "compile": "yarn hardhat compile",
    "truffle": "npx truffle console --network development",
    "deploy": "yarn hardhat run --network milkomedaTestnet scripts/deploy.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.9",
    "hardhat": "^2.9.9"
  },
  "dependencies": {
    "@adaswap-testnet/core": "^0.0.3",
    "@adaswap-testnet/lib": "^0.0.2"
  }
}
